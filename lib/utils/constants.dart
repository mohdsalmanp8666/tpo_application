import 'package:flutter/material.dart';

const primary = Color(0xFF1D4ED8);
const primaryText = Color(0xFF020617);

const EdgeInsets defaultPadding = EdgeInsets.symmetric(
  vertical: 10,
  horizontal: 25,
);
